# TODO

- [ ] Add a FinClarity demo GIF/screenshot next to the "Selected Work" card on
      the homepage (`index.html`, `.work` section) so it doesn't look empty
      next to the card. Blocked on https://getfinclarity.vercel.app/demo being
      behind Vercel deployment protection (redirects to Vercel login) — either
      disable protection, add a bypass token, or supply a manual screenshot.
      For now the card is flush-left instead of centered (`css/style.css`
      `.work-grid`).
